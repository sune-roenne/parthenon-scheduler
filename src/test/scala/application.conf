connections : [
   {
      name : "primary"
      driver-class : "oracle.jdbc.OracleDriver"
	  url : "jdbc:oracle:thin:@myhost:1521:orcl"
	  user : "TestUser"
	  password : "TestUser"
   }
]

holidays: {
   connection : "primary"
   # Must return a relation of a single column of type java.sql.Date
   query : """select * from """
}

irregular-bank-days : {
   connection : "primary"
   # Must return a relation of a single column of type java.sql.Date
   query : """select * from """
}

executions : [
   {
      batch-group-name : "TEST_GROUP"
	  # Format : HH:mm"
	  time-of-day : "08:00"
	  output-file-name : """\\test-share\input-dir\execution.xml"""
	  xml-prefix : ["Request"]
	  group-element-name : "BatchJobGroup"
	  batch-date-element-name : "BatchDate"
	  # Optional. Must be valid scala-expression. Implicits : holidays and bankdays (both : Set[java.sql.Date]) and today : java.sql.Date
	  execution-day-condition : """ """
   }
]